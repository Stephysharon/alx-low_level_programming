#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @one: first number
 * @two: second number
 *
 * Return: one + two
 */
int add(int a, int b)
{
	int c;

	c = a + b;

	return (c);
}
